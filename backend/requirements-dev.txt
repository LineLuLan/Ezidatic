-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
aiosqlite==0.20.0
ruff==0.8.4
black==24.10.0
mypy==1.13.0
# Pure-Python in-memory Redis stand-in for POL-05 cache tests so CI
# doesn't need a Redis service.
fakeredis>=2,<3

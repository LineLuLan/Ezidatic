# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Validation & settings
pydantic[email]==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
# greenlet is a transitive dep of SQLAlchemy[asyncio]. The conditional
# install-requires marker on sqlalchemy 2.0.36 stops triggering on
# Python 3.13, so we pin it explicitly to keep async sessions working
# (`ValueError: greenlet library is required`).
greenlet>=3
alembic==1.14.0

# Storage (prod backend — Supabase Storage; lazy-imported, dev tier is
# unaffected). 2.10 is the current 2.x line as of repo era; range pin
# for minor patches.
supabase>=2.10,<3

# Cache (POL-05). redis.asyncio handles both `redis://` (dev docker)
# and `rediss://` (prod TLS — Upstash). API surface (Redis.from_url,
# get, setex, decode_responses) is stable from 5.x through 7.x.
redis>=5,<8

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# Data
polars==1.18.0
pandas==2.2.3
numpy==2.2.1
openpyxl==3.1.5
# polars.DataFrame.to_pandas() round-trips through pyarrow; not a hard
# polars dep on its own, so pin it explicitly so EDA + ML test paths
# don't blow up with ModuleNotFoundError.
pyarrow>=14

# ML
scikit-learn==1.6.0
lightgbm==4.5.0

# LLM / agents
langchain==0.3.13
langchain-core==0.3.28
groq==0.15.0
google-generativeai==0.8.3
chromadb==0.5.23

# Utilities
httpx==0.28.1
structlog==24.4.0
python-dotenv==1.0.1
